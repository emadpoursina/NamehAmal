{
  "name": "NamehAmal",
  "install": "npm ci --no-audit --no-fund && ([ -f .env ] || cp .env.example .env) && npx prisma generate && npx prisma migrate deploy",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server (webpack) on port 3060"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3060
    }
  ]
}
